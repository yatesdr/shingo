name: Protocol

on:
  push:
    paths:
      - "protocol/**"
  pull_request:
    paths:
      - "protocol/**"
  workflow_call:

# See core.yml for rationale - same idempotent burst-coalescing.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.0"

      - name: Build
        working-directory: protocol
        run: go build ./...

      - name: Prepare build dir
        working-directory: protocol
        run: mkdir -p build

      - name: Test
        working-directory: protocol
        run: go test -v -coverprofile=build/coverage ./...

      - name: Test (race)
        working-directory: protocol
        # Warning-only for the first 30 days (until 2026-06-16). Race
        # findings surface latent production data races. Promote to
        # gating once baseline is known clean. Tracked in Phase 7.
        continue-on-error: true
        run: go test -race -count=1 ./...

      - name: Coverage report
        working-directory: protocol
        run: go tool cover -func=build/coverage
