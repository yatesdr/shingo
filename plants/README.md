# Plant specs (local dev)

Declarative plant specifications consumed by `cmd/seeddev` (brief Phase 4, D2).
`docker-compose.dev.yml` mounts this directory read-only into the `seed` service
at `/plants`. `demo.yaml` (the demo plant — T4.2) lands here in Phase 4.

LOCAL DEV / SIMULATION ONLY.
